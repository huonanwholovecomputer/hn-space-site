# 文章图片大小控制速查（HN Space）

> 你的站点已支持三种图片尺寸控制方式，从「全自动」到「手动单张」，按需选用。
> 本文档为 2026-09 核验后的**实际生效规则**（布局：`layouts/_partials/extend_head.html` 内联样式 + `assets/css/extended/mobile.css` + `static/js/math-image.js`）。

---

## 1. 默认（什么都不写）—— 全自动

```markdown
![示意图](/posts/01-dev-environment-setup/media/image2.png)
```

| 场景 | 显示效果 |
|------|---------|
| 单独一张图（有文字间隔） | 桌面 **88%**；手机（≤768px）**95%**；居中 |
| 连续多张图（一张接一张，如教程步骤截图） | 整组 **80%**，桌面 / 手机一致，紧凑排列 |
| 竖长图 | 宽度沿用上两条规则，高度上限 **50vh**（过高按屏高截断） |
| 任意图 | 点击弹悬浮层，可缩放 / 拖动 / 放大查看原图（放大上限 92% × 90% 屏） |

**「连续」的判定**：图片所在段落与其相邻段落各自只含一张图（按段落相邻判断）；两张图之间隔着一段文字就不算连续，各自按「单独一张」展示。图被 `math-image.js` 在运行时包装为 `.math-img-box`，只有刷新加载完成后自动生效。

## 2. 手动指定单张宽度 —— 用 HTML `<img>` + `style`

想让某张图**固定为 70%宽**（不受全局规则影响）：

```markdown
<img src="/posts/03-deepseek-price-increase/media/openrouter-rank.png" style="width:70%" alt="周榜截图">
```

- `style="width:XX%"` —— XX 填 30~100 之间任意数（相对正文宽度）
- 改完**点击放大的悬浮层仍然有效**（系统会自动包装）
- 这条会**覆盖**全局的 88% / 95% / 80% 规则，并且**不参与「连续多图收窄」**（手动指定宽度的图永远按你写的宽度显示）
- 手动指定宽度后，竖长图的高度限制（50vh）也一并解除

**示例**：整宽 100%、半宽 50%：
```markdown
<img src="图片路径" style="width:100%" alt="全宽图">
<img src="图片路径" style="width:50%" alt="半宽图">
```

## 3. 组合使用 —— 全局 + 手动微调

正文绝大多数图继续用默认规则（省心），只有少数特殊图用手动 `style="width:"` 覆盖。

---

## 常见场景对应

| 你要的效果 | 怎么做 |
|-----------|--------|
| 教程步骤截图一堆，都缩小 | 什么都不写，连续放置自动 **80%** |
| 内文单独一张示意图，居中 | 什么都不写（桌面 88% / 手机 95%） |
| 某张关键大图要占满 | `<img src="..." style="width:100%">` |
| 某张图只要一半宽 | `<img src="..." style="width:50%">` |
| 恢复自动 | 删掉 `style="width:..."` 即可 |

> 提醒：`style="width:70%"` 里的百分比相对**文章正文容器宽度**（约 960px），
> 不是屏幕宽度。想更精细就写像素：`style="width:480px"`。

---

## 规则位置速查（改样式时找这里）

| 规则 | 位置 |
|------|------|
| 桌面 88%、连续组 80%、竖图 50vh、手动宽度豁免、悬浮层 92%/90% | `layouts/_partials/extend_head.html` 内联 `<style>` |
| 手机 95% | `assets/css/extended/mobile.css`（≤768px 媒体查询） |
| 图片包装与点击放大逻辑（`.math-img-box` / lightbox） | `static/js/math-image.js` 的 `enhanceImages` |